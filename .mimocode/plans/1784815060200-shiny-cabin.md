# خطة: إصلاح موقع الأيقونات وتوسيع شريط البحث

## المشكلة
1. الأيقونات (الإشعارات + الملف الشخصي) كانت بعيدة بـ `ml-48`但 قلّلتها إلى `ml-12` بالغلط
2. شريط البحث عليه `flex-1` بس مش بيتسع

## الحل
1. إعادة `ml-48` على الأيقونات اليمنى (المسافة اللي كان المستخدم راضي عنها)
2. شريط البحث فيه `flex-1` بالفعل — هيتوسع تلقائياً في المساحة الفاضية

### الملف: `src/components/navbar.tsx`
- السطر 397: تغيير `ml-12` إلى `ml-48`

## التحقق
- شغّل `bun run dev`
- تأكد إن الأيقونات بعيدة عن شريط البحث
- تأكد إن شريط البحث بياخد المساحة الفاضية
