'use client'

import { MinimalFooter } from '@/components/ui/minimal-footer'

// Footer الأساسي للموقع — يستخدم نفس تصميم MinimalFooter
// Primary: الأقسام / استكشاف / دعم الأقسام / مشاكل وحلول / حول
// Subcategories: كل منصة منفصلة (PS1-5) + السلاسل + الفرق
// تم حذف عمود المجتمع والأيقونات حسب الطلب
export function Footer() {
  return <MinimalFooter />
}
