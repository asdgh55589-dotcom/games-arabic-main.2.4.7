/**
 * NotificationChannel — قنوات التوصيل
 * Pure value object with no external dependencies.
 */

export enum NotificationChannel {
  InApp = 'in_app',
  Email = 'email',
  Telegram = 'telegram',
}
